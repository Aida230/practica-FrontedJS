//esto es como un patron y este es el 1 paso, crear notification-controller
export function notificationController(nodo) {//como parametro recibe un nodo porque todo controllador se tiene que encargar de un nodo del DOM
  //aqui haremos cosas

}